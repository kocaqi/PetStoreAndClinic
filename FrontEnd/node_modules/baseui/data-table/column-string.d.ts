import type { ColumnOptions, SharedColumnOptions } from './types';
declare type Options = {
    lineClamp?: number;
} & SharedColumnOptions<string>;
declare type FilterParameters = {
    description: string;
    exclude: boolean;
};
declare type StringColumn = ColumnOptions<string, FilterParameters>;
declare function StringColumn(options: Options): StringColumn;
export default StringColumn;
