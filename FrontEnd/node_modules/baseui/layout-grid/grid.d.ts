import * as React from 'react';
import type { GridProps, SharedGridProps } from './types';
export declare const GridContext: React.Context<SharedGridProps>;
export default function Grid({ align, behavior, children, gridColumns, gridGaps, gridGutters, gridMargins, gridMaxWidth, gridStyle, gridUnit, overrides, }: GridProps): JSX.Element;
