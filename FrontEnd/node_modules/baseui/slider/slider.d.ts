/// <reference types="react" />
import type { SliderProps } from './types';
declare function Slider({ overrides, disabled, marks, onChange, onFinalChange, min, max, step, persistentThumb, valueToLabel, value: providedValue, }: SliderProps): JSX.Element;
export default Slider;
