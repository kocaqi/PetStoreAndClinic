import * as React from 'react';
import type { StatefulContainerProps, StateReducer, State, Params, ChangeAction } from './types';
declare class StatefulSliderContainer extends React.Component<StatefulContainerProps, State> {
    constructor(props: StatefulContainerProps);
    static defaultProps: {
        stateReducer: StateReducer;
        min: number;
        max: number;
        step: number;
        marks: boolean;
        onChange: () => void;
        onFinalChange: () => void;
    };
    onChange: (params: {
        value: Array<number>;
    }) => unknown;
    onFinalChange: (params: {
        value: Array<number>;
    }) => unknown;
    internalSetState: (type: ChangeAction, { value }: Params) => void;
    render(): React.ReactNode;
}
export default StatefulSliderContainer;
