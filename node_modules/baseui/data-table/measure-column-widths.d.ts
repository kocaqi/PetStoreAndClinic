/// <reference types="react" />
import type { ColumnOptions, Row } from './types';
declare type MeasureColumnWidthsProps = {
    columns: ColumnOptions[];
    isSelectable: boolean;
    onWidthsChange: (a: number[]) => void;
    rows: Row[];
    widths: number[];
};
export default function MeasureColumnWidths({ columns, rows, widths, isSelectable, onWidthsChange, }: MeasureColumnWidthsProps): JSX.Element;
export {};
