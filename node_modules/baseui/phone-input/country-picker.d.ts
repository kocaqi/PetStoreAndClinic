/// <reference types="react" />
import type { CountrySelectProps } from './types';
declare function CountryPicker(props: CountrySelectProps): JSX.Element;
declare namespace CountryPicker {
    var defaultProps: {
        disabled: boolean;
        inputRef: {
            current: any;
        };
        maxDropdownHeight: string;
        maxDropdownWidth: string;
        overrides: {};
        size: "default";
        error: boolean;
        positive: boolean;
        required: boolean;
    };
}
export default CountryPicker;
