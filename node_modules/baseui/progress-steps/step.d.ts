/// <reference types="react" />
import type { StepProps } from './types';
declare function Step({ overrides, isCompleted, isActive, isLast, title, children }: StepProps): JSX.Element;
declare namespace Step {
    var defaultProps: {
        isCompleted: boolean;
        isLast: boolean;
    };
}
export default Step;
