import * as React from 'react';
import type { TimezonePickerProps, TimezonePickerState, Timezone } from './types';
declare class TimezonePicker extends React.Component<TimezonePickerProps, TimezonePickerState> {
    state: TimezonePickerState;
    componentDidMount(): void;
    componentDidUpdate(prevProps: TimezonePickerProps): void;
    buildTimezones: (compareDate: Date) => Timezone[];
    render(): JSX.Element;
}
export default TimezonePicker;
