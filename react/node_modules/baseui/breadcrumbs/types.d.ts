import type { ReactNode } from 'react';
import type { Override } from '../helpers/overrides';
export declare type BreadcrumbsOverrides = {
    Root?: Override;
    Separator?: Override;
    List?: Override;
    ListItem?: Override;
    Icon?: Override;
};
export declare type BreadcrumbsProps = {
    children?: ReactNode;
    overrides?: BreadcrumbsOverrides;
    ariaLabel?: string;
    'aria-label'?: string;
    /** Whether to show a trailing separator after the last breadcrumb. */
    showTrailingSeparator?: boolean;
};
