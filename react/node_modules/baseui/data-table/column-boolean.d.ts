import type { ColumnOptions, SharedColumnOptions } from './types';
declare type Options = {} & SharedColumnOptions<boolean>;
declare type FilterParameters = {
    selection: Set<boolean>;
    description: string;
    exclude: boolean;
};
declare type BooleanColumn = ColumnOptions<boolean, FilterParameters>;
declare function BooleanColumn(options: Options): BooleanColumn;
export default BooleanColumn;
