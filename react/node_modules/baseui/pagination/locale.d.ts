export declare type PaginationLocale = {
    prev: string;
    next: string;
    preposition: string;
};
declare const locale: {
    prev: string;
    next: string;
    preposition: string;
};
export default locale;
