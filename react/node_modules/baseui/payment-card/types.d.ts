import type { InputOverrides, InputProps, StInputPropsDiff } from '../input';
import type { Override } from '../helpers/overrides';
export declare type PaymentCardOverrides = {
    IconWrapper?: Override;
} & InputOverrides;
export declare type PaymentCardProps = {
    overrides: PaymentCardOverrides;
} & InputProps;
export declare type StatefulPaymentCardProps = {
    overrides?: PaymentCardOverrides;
} & StInputPropsDiff;
