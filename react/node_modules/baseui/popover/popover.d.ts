import * as React from 'react';
import type { PopoverProps } from './types';
declare const Popover: {
    (props: PopoverProps & {
        innerRef?: React.Ref<HTMLElement>;
    }): JSX.Element;
    defaultProps: Partial<import("./types").BasePopoverProps>;
};
export default Popover;
