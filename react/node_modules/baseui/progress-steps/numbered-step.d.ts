/// <reference types="react" />
import type { NumberedStepProps } from './types';
declare function NumberedStep({ overrides, isCompleted, isActive, isLast, title, step, children, }: NumberedStepProps): JSX.Element;
declare namespace NumberedStep {
    var defaultProps: {
        isCompleted: boolean;
        isLast: boolean;
    };
}
export default NumberedStep;
