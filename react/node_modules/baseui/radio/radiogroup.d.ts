import * as React from 'react';
import type { RadioGroupProps, DefaultProps, StatelessState } from './types';
import type { ChangeEvent } from 'react';
declare class StatelessRadioGroup extends React.Component<RadioGroupProps, StatelessState> {
    static defaultProps: DefaultProps;
    state: {
        isFocusVisible: boolean;
        focusedRadioIndex: number;
    };
    handleFocus: (event: ChangeEvent<HTMLInputElement>, index: number) => void;
    handleBlur: (event: ChangeEvent<HTMLInputElement>, index: number) => void;
    render(): JSX.Element;
}
export default StatelessRadioGroup;
